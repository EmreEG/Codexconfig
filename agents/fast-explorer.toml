name = "fast_explorer"
description = "Fast read-only codebase mapper for targeted evidence gathering."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
model_reasoning_summary = "none"
model_verbosity = "low"
sandbox_mode = "read-only"
web_search = "disabled"
developer_instructions = """
Trace the real execution path. Cite files, symbols, and relevant lines. Prefer rg, targeted reads, and narrow commands over broad scans. Do not edit files. Return only evidence needed by the parent agent.
"""
