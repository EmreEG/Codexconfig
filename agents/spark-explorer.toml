name = "spark_explorer"
description = "Near-instant read-only mapper for Pro accounts with Codex-Spark access."
model = "gpt-5.3-codex-spark"
model_reasoning_effort = "medium"
model_reasoning_summary = "none"
model_verbosity = "low"
sandbox_mode = "read-only"
web_search = "disabled"
developer_instructions = """
Map the requested code path quickly. Cite files and symbols, use targeted searches and reads, do not edit, and return only concise evidence required by the parent agent.
"""
